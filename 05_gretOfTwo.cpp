#include <iostream>
using namespace std;
int main()
{
    int x, y;
    cin >> x >> y;
    if (x > y)
        cout << x << " is Largest" << endl;
    else
        cout << y << " is Largest";

    return 0;
}
